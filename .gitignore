# Dependências
/node_modules

# Next.js (build/cache NÃO deve ir pro git)
/.next/
/out/

# Builds
/build
/dist

# Arquivos de ambiente
# ⚠️ repo privado + Vercel: o .env fica VERSIONADO (contém DATA_DIR, ADMIN_KEY, SESSION_SECRET)
# Só overrides locais / backups ficam fora do git:
.env.local
.env.*.local
.env.bak*
settings.local.json

# Logs do dev server / npm
*.log
start.log
nohup.out
.devserver.*

# TypeScript / build info
*.tsbuildinfo
*.tgz

# Sistema
.DS_Store
Thumbs.db

# Editores
.vscode/*
!.vscode/extensions.json
.idea/

# Temporários
_typecheck.txt

# Uploads gerados em runtime (comprovantes PIX, QR codes)
/public/uploads/proofs/
/public/uploads/donate/

# Banco de dados SQLite local (legado — migrado para JSON em data/*.json)
/data/*.db
/data/*.db-shm
/data/*.db-wal
/data/*.tmp

# Backups
/backups/
    