# Dependências
/node_modules

# Next.js (build/cache NÃO deve ir pro git — foi a causa do cache corrompido)
/.next/
/out/

# Builds
/build
/dist

# Arquivos de ambiente / segredos
.env
.env.*
!.env.example
.env.local

# Logs do dev server / npm
*.log
start.log
nohup.out
.devserver.*

# TypeScript / build info
*.tsbuildinfo
*.tgz

# Sistema
.DS_Store
Thumbs.db

# Editores
.vscode/*
!.vscode/extensions.json
.idea/

# Temporários
_typecheck.txt

# Uploads gerados em runtime (comprovantes PIX, QR codes)
# Obs.: as músicas de ilha (public/uploads/music) são assets versionados e
# ficam de fora desta regra.
/public/uploads/proofs/
/public/uploads/donate/

# Banco de dados SQLite local
/data/*.db
/data/*.db-wal
/data/*.db-shm

# Backups automáticos do banco (scripts/backup-db.mjs)
/backups/