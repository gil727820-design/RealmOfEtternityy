/*
 * Persistência 100% local com arquivos JSON (data/*.json).
 * Zero custo, zero servidor externo, zero dependência nativa.
 * Substituiu o SQLite (better-sqlite3 + drizzle-orm).
 */
export * from "./jsonStore";